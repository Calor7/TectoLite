import {describe,expect,it} from 'vitest';import {validateCanonicalHeightmapOptions} from './export';
describe('canonical heightmap validation',()=>{it('requires an even integer width of at least two',()=>{expect(validateCanonicalHeightmapOptions(1,-1,1)).toMatch(/even/);expect(validateCanonicalHeightmapOptions(3,-1,1)).toMatch(/even/);expect(validateCanonicalHeightmapOptions(2,-1,1)).toBeNull();});it('requires a finite ascending meter range',()=>{expect(validateCanonicalHeightmapOptions(4,1,1)).toMatch(/Minimum/);expect(validateCanonicalHeightmapOptions(4,Infinity,2)).toMatch(/Minimum/);expect(validateCanonicalHeightmapOptions(4,-11000,9000)).toBeNull();});});
